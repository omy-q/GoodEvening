package com.example.goodevening.superview.view.mainview

import com.example.goodevening.domainmodel.CategoryFilm

interface OnMoreButtonClickListener {
    fun onMoreButtonClick(category : CategoryFilm)
}